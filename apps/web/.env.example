# ─── Sonda Note · Next.js Web Dashboard ───
# Copy to apps/web/.env.local and update values.

# FastAPI backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Chrome Extension ID (from chrome://extensions after loading unpacked extension build)
# Allows the dashboard to sync JWT auth token directly with the extension
NEXT_PUBLIC_EXTENSION_ID=edmgnmhmpamhdjocmappgnakfoiblege
